[v0.6.12.2 변경사항]
- 테스트 종료 후 실제 캠페인 시작 전에 NAS 집계 결과를 전체 초기화할 수 있는 reset_test_data.sh 추가
- 초기화 스크립트는 실행 전 RESET 문구를 직접 입력해야 동작하도록 안전 확인 절차 적용
- 초기화 시 team-mbti-api 컨테이너를 잠시 중지한 뒤 SQLite DB(team_mbti.db 및 WAL/SHM 파일)를 삭제하고 다시 시작
- README에 운영 시작 전 테스트 결과 초기화 절차 추가
- 프론트엔드(index.html, config.js, app.min.js, style.css) 및 assets는 수정하지 않음
- 스크립트 파일을 NAS에 복사하는 것만으로는 Docker 재시작 불필요

[v0.6.12.1 변경사항]
- Tailscale Funnel 외부 포트를 443 → 8443으로 변경한 설정에 맞춰 config.js의 NAS API 주소 수정
- API_BASE_URL: https://ljknas.tail9251fa.ts.net:8443
- 브라우저 캐시 갱신을 위해 index.html의 config.js 쿼리 버전을 0.6.12.1로 변경
- app.min.js, style.css, assets 및 NAS 백엔드는 수정하지 않음
- Docker 컨테이너 재시작 불필요

[v0.6.12 변경사항]
- 개인 MBTI 검사 진입 시 1번 문항보다 먼저 '소속 팀(지부) 선택' 단계를 별도 화면으로 표시
- 소속 팀(지부)을 선택해야 '검사 시작하기' 버튼이 활성화되도록 변경
- 검사 시작 후에는 선택한 소속 팀(지부)을 상단에 표시하고 '소속 변경' 가능
- '처음부터' 선택 시 문항을 초기화하고 소속 팀(지부) 선택 단계로 복귀
- NAS 팀 목록 불러오기 → 개인 진단 → 결과 익명 자동 전송 → 팀 결과 자동 조회 기능을 실제 app.min.js에 반영
- Tailscale Funnel API 주소(https://ljknas.tail9251fa.ts.net) 연결 유지
- 상단/하단 개인정보 안내 문구를 실제 자동 집계 방식에 맞게 수정
- assets 폴더 및 이미지 파일은 수정하지 않음
- NAS 백엔드 파일 및 teams.json은 수정하지 않음: 컨테이너 재시작 불필요
- 패치 ZIP의 파일은 GitHub 저장소 최상위 폴더에 바로 덮어쓰도록 구성(별도 frontend_patch 폴더 없음)

[v0.6.9.2 변경사항]
- 문항 화면 순서를 다시 고정: 상황 이미지 → 문항 번호/질문 → 선택지
- 문항 번호(01, 02...) 글자 크기를 질문 제목과 동일하게 조정
  · PC: 25px
  · 모바일(820px 이하): 21px
- 브라우저 캐시 갱신을 위해 style.css / app.min.js 버전을 0.6.9.2로 변경
- assets 폴더 및 이미지 파일은 수정하지 않음

# 우리 팀(지부) 업무 MBTI

## 운영 시작 전 테스트 결과 전체 초기화
테스트 기간에 누적된 결과를 삭제하고 실제 캠페인을 0명부터 시작할 때 사용합니다.

### 권장 방법: 초기화 스크립트 사용
1. 이 패치의 `nas_backend/reset_test_data.sh` 파일을 NAS의 `/docker/team-mbti-api/` 폴더에 복사합니다.
2. SSH로 NAS에 접속합니다.
3. 아래 명령을 실행합니다.

```bash
cd /volume1/docker/team-mbti-api
sudo sh reset_test_data.sh
```

4. 화면에 확인 문구가 나오면 `RESET`을 정확히 입력합니다.
5. 스크립트가 `team-mbti-api` 컨테이너를 중지하고 `data/team_mbti.db`, `team_mbti.db-wal`, `team_mbti.db-shm` 등을 삭제한 뒤 컨테이너를 다시 시작합니다.
6. 초기화 후 `/api/team-results?team_code=...`에서 참여인원이 0명으로 표시되는지 확인합니다.

※ 이 작업은 MBTI 전용 DB만 삭제합니다. NAS의 다른 공유폴더, 사진, 문서, 백업 데이터에는 접근하거나 변경하지 않습니다.
※ 실제 운영 데이터가 누적된 이후 실행하면 그 결과도 모두 삭제되므로 캠페인 시작 전 테스트 데이터 정리에만 사용하세요.


## 적용 방법
1. 이 패치 ZIP의 파일을 기존 GitHub 저장소 최상위 폴더에 덮어씁니다.
2. `frontend_patch`라는 하위 폴더를 새로 만들지 않습니다.
3. Commit → Push 후 GitHub Pages에서 반영 여부를 확인합니다.
4. v0.6.12는 NAS 백엔드 파일을 수정하지 않으므로 Docker 컨테이너 재시작이 필요 없습니다.

## GitHub Pages에서 사용하는 파일
- `index.html`
- `style.css`
- `config.js`
- `app.min.js`
- `assets/` (이번 패치에서는 수정하지 않음)

## 개인정보 및 집계 관련
- 로그인 기능 없음
- 이름/사번 등 개인 식별정보 수집 없음
- 개인 MBTI 계산은 브라우저에서 수행
- 검사 완료 시 소속 팀(지부), MBTI 결과, 제출시각, 중복 참여 방지용 익명 식별값을 NAS 집계 서버에 저장
- 동일 브라우저/기기에서 다시 참여하면 새 인원으로 계속 추가하지 않고 기존 익명 결과를 갱신

## 현재 포함 기능
- 업무 상황형 MBTI 20문항
- 개인 MBTI 및 강점/점검 필요 부분 결과
- 검사 시작 전 소속 팀(지부) 선택
- 개인 결과 NAS 익명 자동 집계
- 팀별 대표 MBTI 및 SJ·SP·NT·NF 기질 분석
- 팀 결과 3명 이상 참여 시 공개
- 기존 수동 팀 MBTI 입력 로직은 예비용으로 유지하되 화면에서는 숨김
- SJ/SP/NT/NF 기질 분석
- 팀 강점 및 맹점 표시
- 개선과제 예시 안내

※ 사내 캠페인용 프로토타입이며 정식 MBTI 심리검사가 아닙니다.


## v0.3 브랜드 디자인 반영
- 제공된 The-K 브랜드관리 매뉴얼의 메인 컬러를 UI에 반영
  - The-K Blue: RGB 33,64,154 (#21409A)
  - The-K Red: RGB 237,27,47 (#ED1B2F)
  - Ivory / Light Gray / Dark Gray 보조색 적용
- 권장서체에 맞춰 Noto Sans CJK KR / Noto Sans 우선 적용(미설치 환경에서는 시스템 한글 고딕으로 대체)
- 상단 TEAM 표기를 공식 The-K 심볼 이미지로 교체
- 외부 폰트/API 호출 없이 기존의 브라우저 단독 실행 구조 유지


## v0.4 변경사항
- 메인 히어로 설명문을 데스크톱에서 한 줄로 표시
- 모바일 화면에서는 자동 줄바꿈 유지


## v0.5 변경사항
- Noto Sans KR 자체 호스팅(webfont) 구조 추가
- assets/fonts 폴더 추가
- 아래 TTF 3개를 직접 넣으면 기기 설치 여부와 관계없이 동일 폰트 사용
  - NotoSansKR-Regular.ttf
  - NotoSansKR-Medium.ttf
  - NotoSansKR-Bold.ttf
- CSS @font-face에서 400 / 500 / 700 굵기 지정
- 외부 Google Fonts/CDN 사용 없음


## v0.5.1 변경사항
- v0.5 기준 유지
- 메인 파란색 히어로 영역의 마름모 장식 제거
- 히어로 배경을 The-K Blue 단색으로 통일


## v0.5.2 변경사항
- v0.5.1 디자인을 기준으로 개인검사 기능 개선
- 개인검사 16문항 → 20문항
- 각 지표(E/I, S/N, T/F, J/P)별 5문항으로 균등 구성
- 2지선다 → 3지선다
- 회식, 장기자랑, 로또, 휴가 등 공식 사내 캠페인에 부적합한 문항 제외
- 업무 인수인계, 회의, 보고서, 피드백, 평가, 업무계획 등 실제 업무상황 중심으로 구성
- 개인검사를 한 페이지 전체 나열 방식에서 한 문항씩 이동하는 방식으로 변경
- 일부 문항은 선택지 방향을 반대로 배치해 특정 MBTI 결과를 쉽게 유추하기 어렵게 구성
- 팀 분석 및 개선 연결 기능은 기존 구조 유지


## v0.6.3 변경사항
- 개인 MBTI 검사 문항 화면에 문항별 일러스트 배치를 위한 와이어프레임(이미지 삽입 영역) 추가
- 실제 이미지는 아직 미삽입 상태이며, 빈 상자 형태로 위치와 크기만 설계


## v0.6.4 변경사항
- 개인 MBTI 검사 문항의 이미지 영역을 모바일 기준 세로형(portrait) 와이어프레임으로 확대
- 문항 상황 이미지 예시로 회사 오리 마스코트 시안을 반영
- 향후 20개 문항별 상황에 맞춰 동일 마스코트 스타일의 일러스트를 삽입할 수 있도록 구조 정리


## v0.6.5 변경사항
- 개인검사 문항 상단의 "문항 상황 일러스트" / 축 설명 라벨 삭제
- 이미지 영역을 가로로 더 넓게 조정하여 제목 폭에 더 가깝게 보이도록 수정
- 모바일에서도 꽉 차게 보이도록 이미지 영역 최대 폭과 비율 조정


## v0.6.6 변경사항
- 1~3번 개인검사 문항에 오리 마스코트 기반 상황 이미지를 실제로 적용
- 4번 이후 문항은 기존 이미지 영역(와이어프레임) 구조를 유지
- 문항 이미지가 카드에 자연스럽게 꽉 차 보이도록 이미지 표시 스타일 보정

## v0.6.10 변경사항
- NAS 자동 집계 프로토타입 추가
- 개인검사 전에 소속 팀(지부)을 선택하는 UI 추가
- 검사 완료 시 `팀 코드 + MBTI 결과 + 익명 기기 식별값`만 NAS API로 자동 전송
- 팀(지부) 분석 화면을 NAS 자동 집계 결과 조회 방식으로 전환
- 팀 결과는 3명 이상 참여 시 표시
- NAS 주소는 `config.js`에서 별도 관리하며, 미설정/연결 실패 시 개인검사는 기존처럼 계속 사용 가능
- 기존 수동 팀 MBTI 입력 UI는 화면에서 숨기되 로직은 예비용으로 유지
- assets 폴더 및 이미지 파일은 수정하지 않음
